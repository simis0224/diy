<!DOCTYPE html>
<html>
    <head>
        <title>首页</title>
        <link rel='stylesheet' href='/stylesheets/main.css' />
    </head>
    <body>
        <% include header %>
        <div class="content">
            <% if (locals.currentUser) {%>
                <a href="/createPost">发布作品</a><br>
                <a href="/listPost/<%= currentUser.username %>">我的所有作品</a><br>
                <a href="/editUser/<%= currentUser.username %>">更新用户档案</a><br>
                <a href="/listUser">查看所有用户</a><br>
            <%} else {%>
                <a href="/login">登陆</a>
                <a href="/signup">注册用户</a>
            <%}%>

            <% if (message) { %>
                <p><%= message %></p>
            <% } %>
        </div>
        <% include footer %>
    </body>
</html>