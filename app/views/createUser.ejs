<!DOCTYPE html>
<html>
    <head>
        <title>用户注册</title>
        <link rel='stylesheet' href='/stylesheets/main.css' />
        <link rel='stylesheet' href='/stylesheets/login.css' />
    </head>
    <body>
        <% include header %>
        <div class="content">
            <form method="post" id="signupForm" action="/signup" class="signup">
                <h2>注册</h2>
                <input type="text" name="email" value="<% if(locals.user && locals.user.email) {%><%= user.email %><% } %>" placeholder="电子邮件" /><br>
                <input type="text" name="username" value="<% if(locals.user && locals.user.username) {%><%= user.username %><% } %>" placeholder="用户名"/><br>
                <input type="password" name="password" placeholder="账户密码"/><br>
                <p class="error">
                    <%= message %>
                </p>
                <button type="submit" name="update">注册</button><br>
            </form>
            <div class="otherLink">
                <p>
                    已有账号，<a href="/login">从这里登录</a>
                </p>
            </div>
        </div>
        <% include footer %>
    </body>
</html>