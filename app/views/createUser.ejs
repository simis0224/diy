<!DOCTYPE html>
<html>
    <head>
        <title>用户注册</title>
        <link rel='stylesheet' href='/stylesheets/main.css' />
        <link rel='stylesheet' href='/stylesheets/login.css' />
    </head>
    <body>
        <% include header %>
        <div class="content">
            <form method="post" id="signupForm" action="/signup" class="signup">
                <h2>注册</h2>
                <input type="text" name="email" value="<% if(locals.item && locals.item.email) {%><%= item.email %><% } %>" placeholder="电子邮件" />
                <input type="text" name="itemname" value="<% if(locals.item && locals.item.username) {%><%= item.username %><% } %>" placeholder="用户名"/>
                <input type="password" name="password" placeholder="账户密码"/>
                <p class="error">
                    <%= message %>
                </p>
                <button type="submit" name="update">注册</button><br>
            </form>
            <div class="otherLink">
                <p>
                    已有账号，<a href="/login">从这里登录</a>
                </p>
            </div>
        </div>
        <% include footer %>
    </body>
</html>