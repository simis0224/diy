<!DOCTYPE html>
<html>
	<head>
		<title>用户注册</title>
		<link rel='stylesheet' href='/stylesheets/main.css' />
	</head>
	<body>
        <% include header %>
        <div class="content">
            <p><%= message %></p>
            <% if(locals.item) { %>
                <form method="post" id="editForm" action="/editUser/<%= item.username %>">
                    电子邮件：<input type="text" name="email" value="<%= item.email %>" aria-label="电子邮件"/><br>
                    用户名：<input type="text" name="username" value="<%= item.username %>" aria-label="用户名"/><br>
                    密码：<input type="password" name="password" aria-label="账户密码"/><br>
                    <input type="hidden" name="id" value="<%= item._id %>"/>
                    <button type="submit" name="signUp">更新</button><br>
                </form>
            <% } %>
        </div>
        <% include footer %>
	</body>
</html>