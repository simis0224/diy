<!DOCTYPE html>
<html>
	<head>
		<title>用户注册</title>
		<link rel='stylesheet' href='/stylesheets/main.css' />
	</head>
	<body>
        <% include header %>
        <div class="content">
            <p><%= message %></p>
            <% if(locals.user) { %>
                <form method="post" id="editForm" action="/editUser/<%= user.username %>">
                    电子邮件：<input type="text" name="email" value="<%= user.email %>" aria-label="电子邮件"/><br>
                    用户名：<input type="text" name="username" value="<%= user.username %>" aria-label="用户名"/><br>
                    密码：<input type="password" name="password" aria-label="账户密码"/><br>
                    <input type="hidden" name="id" value="<% if(locals.user) {%><%= user._id %><% } %>"/>
                    <button type="submit" name="signUp">更新</button><br>
                </form>
            <% } %>
        </div>
        <% include footer %>
	</body>
</html>