<!DOCTYPE html>
<html>
    <head>
        <title>首页</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <% if(locals.isLoggedIn) {%>
            你好, <% if (username) { %><%= username %><% } %><br>
            <a href="/createPost">发布作品</a><br>
            <a href="/listPosts/<% if (username) { %><%= username %><% } %>">我的所有作品</a><br>
            <a href="/editUser/<% if (username) { %><%= username %><% } %>">更新用户档案</a><br>
            <a href="/logout">登出</a><br>
        <%} else {%>
            <a href="/login">登陆</a>
            <a href="/signup">注册用户</a>
        <%}%>

        <% if (message) { %>
            <p><%= message %></p>
        <% } %>
    </body>
</html>