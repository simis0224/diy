<!DOCTYPE html>
<html>
    <head>
        <title>发布作品</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>

    <form method="post" id="post" action="/createPost">
        名称：<input type="text" name="email" value="<% if(locals.post && locals.post.subject) {%><%= post.subject %><% } %>" aria-label="名称"/><br>
        主图：<input type="text" name="userName" value="<% if(locals.post && locals.post.pic) {%><%= post.pic %><% } %>" aria-label="主图"/><br>
        描述：<textarea name="body" aria-label="描述" rows="4" cols="50"></textarea><br>
        <button type="submit" name="update">发布</button><br>
    </form>

    <% if(locals.message) {%>
        <%= message %>
    <% } %>
    </body>
</html>