<!DOCTYPE html>
<html>
    <head>
        <title>所有作品</title>
        <link rel='stylesheet' href='/stylesheets/main.css' />
    </head>
    <body>
        <% include header %>
        <div class="content">
            <p><%= message %></p>
            <% if(locals.items) {%>
                <% for(var i = 0; i < items.length; i++) {%>
                    标题：<a href="/viewPost/<%= items[i].id %>" ><%= items[i].subject %></a>
                    图片：<img src="<%= items[i].pic %>" />
                    发布时间：<%=  items[i].createdDate %><br>
                <% } %>
            <% } %>

        </div>
        <% include footer %>
    </body>
</html>